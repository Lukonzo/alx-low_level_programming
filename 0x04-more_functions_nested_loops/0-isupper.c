#include "main.h"

/**
 * _isupper - Checks for upper case character.
 *
 * @x: input character.
 *
 * Return: 1 for upper case or 0 otherwise.
 */
int _isupper(int x)
{
	if(x => 70 && x <= 102)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
