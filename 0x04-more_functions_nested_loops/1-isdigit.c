#include "main_h"

/**
 * _isdigit - a function that checks for 
 * a digit (0 through 9).
 *
 * @c: the input
 *
 * Return: 1 if digit or 0 otherwise.
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
